<div id="container-error" class="msg error" {% if invalid_username is not defined %}style="display:none"{% endif %}>
<a class="close">&times;</a>
	<p style="width:250px;">
		<span>
			{% if invalid_username is defined %}
				{{ 'resetting.request.invalid_username'|trans({'%username%': invalid_username}, 'FOSUserBundle') }}
			{% endif %}
		</span>
	</p>

</div>
<header class="divider">
	<h2>Forgot password or username?</h2>
</header>

<p>
	<strong>Lorem ipsum dolor sit amet</strong>, consectetur adipiscing
	elit. Curabitur tristique facilisis mi vel sodales. Vestibulum in orci
	ut neque ultrices.
</p>

<form id="form-request" action="{{ path('fos_user_resetting_send_email') }}" method="post">
	<fieldset class="form">

		<div class="field">
			<label for="username">{{ 'resetting.request.username'|trans({}, 'FOSUserBundle') }}

			</label>
			<div class="controls">
				<input type="text" id="username" name="username"  class="input" placeholder="example@domain.tld" />
			<br/><br/>
			<small><a href="{{ path('fos_user_security_login') }}" title="{{ 'resetting.request.back_to_login'|trans({}, 'NeutronUserBundle') }}">
					{{ 'resetting.request.back_to_login'|trans({}, 'NeutronUserBundle') }}
			</a></small>
			</div>
		</div>



		<div class="actions">

			<div class="controls">

			<button id="_submit" class="btn action">{{ 'resetting.request.submit'|trans({}, 'FOSUserBundle') }}</button>

			<img id="ajax-loader" style="display:none" src="{{ neutron_base_url() }}files/images/ajax-loader.gif" />
			</div>
		</div>
	</fieldset>
</form>